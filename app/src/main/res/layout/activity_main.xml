<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.chus.aplicaciontrivial.interfaz.MainActivity">

    <TextView
        android:text="TriviaQuiz"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textViewTitulo"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp"
        android:textSize="36sp"
        android:textStyle="normal|bold" />

    <Button
        android:text="Jugar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonJugar"
        android:elevation="0dp"
        android:onClick="empezarPartida"
        android:layout_above="@+id/buttonRanking"
        android:layout_alignStart="@+id/buttonRanking"
        android:layout_marginBottom="32dp"
        android:layout_alignEnd="@+id/textViewTitulo" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/ic_launcher"
        android:id="@+id/imageView"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/textViewTitulo"
        android:layout_alignStart="@+id/buttonJugar"
        android:layout_alignEnd="@+id/textViewTitulo"
        android:layout_above="@+id/buttonJugar" />

    <Button
        android:text="Ranking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonRanking"
        android:onClick="verRanking"
        android:layout_marginBottom="47dp"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/textViewTitulo"
        android:layout_alignEnd="@+id/textViewTitulo" />
</RelativeLayout>
